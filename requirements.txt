# Основные зависимости
python-telegram-bot[job-queue]>=20.0
pybit>=5.0.0
python-dotenv>=1.0.0

# Базовые библиотеки (обычно входят в стандартную поставку Python)
# sqlite3 — встроен в Python
# json — встроен в Python
# re — встроен в Python
# datetime — встроен в Python
# logging — встроен в Python
# os — встроен в Python
